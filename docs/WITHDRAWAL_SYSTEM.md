# Система вывода средств для инфлюенсеров

## Что реализовано:

### 1. База данных
- **withdrawal_requests.sql** - таблица заявок на вывод с функцией обработки
- Поддерживает два способа выплаты: Kaspi Gold и банковская карта
- Автоматическое списание с баланса при одобрении
- Статусы: pending, approved, rejected

### 2. Роли пользователей
- **accountant** - новая роль "бухгалтер"
- Админ может назначать/снимать роль бухгалтера
- Бухгалтер имеет доступ к заявкам на вывод

### 3. Страницы

#### WithdrawalPage.jsx - Страница для инфлюенсера
- Показывает текущий баланс
- Форма создания заявки на вывод (мин. 100 сом)
- Выбор способа получения (Kaspi/Карта)
- История своих заявок
- Статус каждой заявки

#### AccountantPanel.jsx - Страница для бухгалтера
- Список всех заявок на вывод
- Фильтры: Ожидают / Одобренные / Отклоненные / Все
- Полные реквизиты для перевода
- Кнопки "Одобрить" и "Отклонить"
- При одобрении - автоматическое списание с баланса

#### AdminPanel.jsx - Обновления
- Новая вкладка "💰 Выплаты" 
- Кнопка "👔 Назначить бухгалтером" для каждого пользователя
- Админ также может обрабатывать заявки

### 4. Маршруты
- `/withdrawal` - страница вывода средств для инфлюенсера
- `/accountant` - панель бухгалтера

## Установка:

### Шаг 1: Выполнить SQL в Supabase
Откройте SQL Editor в Supabase и выполните:
```sql
d:\TelegramWebApp\supabase\withdrawal_requests.sql
```

### Шаг 2: Назначить бухгалтера
1. Зайдите как админ: `/admin`
2. Найдите нужного пользователя
3. Нажмите "👔 Назначить бухгалтером"
4. Теперь этот пользователь может зайти на `/accountant`

## Процесс работы:

1. **Инфлюенсер:**
   - Заходит в дашборд → кнопка "💸 Вывести"
   - Заполняет форму: сумма + реквизиты
   - Отправляет заявку
   - Видит статус в истории

2. **Бухгалтер:**
   - Заходит на `/accountant`
   - Видит новые заявки во вкладке "⏳ Ожидают"
   - Смотрит реквизиты
   - Переводит деньги вручную (Kaspi/Банк)
   - Нажимает "✅ Одобрить" - деньги списываются с баланса
   - Или "❌ Отклонить" - баланс не меняется

3. **Админ:**
   - Может назначать бухгалтеров
   - Может сам обрабатывать выплаты
   - Видит все заявки во вкладке "💰 Выплаты"

## Безопасность:
- RLS политики разрешают:
  - Инфлюенсерам - только свои заявки
  - Бухгалтерам и админу - все заявки
- Списание происходит только при статусе "approved"
- Проверка достаточности баланса перед списанием
- Транзакция записывается в таблицу transactions

## Файлы:
- `supabase/withdrawal_requests.sql` - SQL схема
- `src/pages/WithdrawalPage.jsx` - страница инфлюенсера
- `src/pages/AccountantPanel.jsx` - страница бухгалтера
- `src/pages/AdminPanel.jsx` - обновлена
- `src/pages/InfluencerDashboard.jsx` - добавлена кнопка вывода
- `src/App.jsx` - новые роуты
