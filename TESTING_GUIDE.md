# 🧪 Тестирование системы одобрения публикаций

## Подготовка

### 1. Выполните SQL скрипт
Откройте Supabase SQL Editor и выполните:
```bash
supabase/task_tracking.sql
```

Этот скрипт создаст:
- ✅ Таблицу `task_submissions`
- ✅ Функцию `approve_submission()`
- ✅ Функцию `update_submission_progress()`
- ✅ RLS политики

### 2. Проверьте таблицу
```sql
SELECT * FROM task_submissions LIMIT 5;
```

### 3. Создайте тестовые данные

Вам понадобятся:
- 👤 **2 пользователя**: один заказчик, один инфлюенсер
- 📋 **1 задание** с целевыми метриками
- ✅ **1 принятый отклик** инфлюенсера

## Сценарий 1: Полный workflow (Happy Path)

### Шаг 1: Заказчик создает задание
1. Откройте приложение как заказчик
2. Нажмите "Создать задание"
3. Заполните форму:
   - Название: "Реклама нового продукта"
   - Описание: "Нужен пост с обзором продукта"
   - Бюджет: 5000 сом
   - **Целевые метрики:**
     - Просмотры: 100,000
     - Лайки: 5,000
     - Комментарии: 500
4. Нажмите "Опубликовать"

**Ожидаемый результат:**
- ✅ Задание создано с `status: 'open'`
- ✅ `target_metrics` сохранены в JSONB

### Шаг 2: Инфлюенсер откликается
1. Откройте приложение как инфлюенсер
2. Перейдите в "Найти заказы"
3. Откройте созданное задание
4. Напишите сопроводительное сообщение
5. Нажмите "Откликнуться"

**Ожидаемый результат:**
- ✅ Отклик создан с `status: 'pending'`

### Шаг 3: Заказчик принимает отклик
1. Откройте приложение как заказчик
2. Перейдите в "Мои задания"
3. Откройте задание
4. Просмотрите отклики
5. Нажмите "Принять" на отклике инфлюенсера

**Ожидаемый результат:**
- ✅ Статус отклика → `accepted`
- ✅ Статус задания → `in_progress`

### Шаг 4: Инфлюенсер отправляет публикацию
1. Откройте приложение как инфлюенсер
2. Создайте тестовый пост в Instagram (или используйте существующий)
3. Скопируйте ссылку, например: `https://www.instagram.com/p/ABC123xyz/`
4. Вернитесь в приложение
5. Откройте задание
6. Нажмите "📤 Отправить публикацию"
7. Вставьте ссылку на пост
8. Нажмите "Отправить на проверку"

**Ожидаемый результат:**
- ✅ Запись создана в `task_submissions`
- ✅ `status: 'pending_approval'`
- ✅ Алерт: "Публикация отправлена на проверку заказчику"

**SQL проверка:**
```sql
SELECT * FROM task_submissions 
WHERE task_id = 'YOUR_TASK_ID' 
ORDER BY submitted_at DESC;
```

### Шаг 5: Заказчик видит уведомление
1. Откройте приложение как заказчик
2. Перейдите в "Мои задания"
3. Откройте задание

**Ожидаемый результат:**
- ✅ Желтый баннер "📤 Новая публикация на проверке"
- ✅ Кнопка "🔍 Проверить публикацию"

### Шаг 6: Заказчик проверяет публикацию
1. Нажмите "🔍 Проверить публикацию"
2. Откроется страница `/client/task/:taskId/review`

**Ожидаемый результат:**
- ✅ Отображается ссылка на Instagram пост
- ✅ Информация об инфлюенсере
- ✅ Целевые метрики
- ✅ Две кнопки: "✅ Одобрить" и "❌ Отклонить"

### Шаг 7: Заказчик одобряет публикацию
1. Нажмите на ссылку Instagram (откроется в новой вкладке)
2. Проверьте пост
3. Вернитесь в приложение
4. Нажмите "✅ Одобрить"

**Ожидаемый результат:**
- ✅ Вызывается `approve_submission()` RPC
- ✅ Статус → `in_progress`
- ✅ Поле `approved_at` заполнено
- ✅ Поле `approved_by` = заказчик
- ✅ Алерт: "Публикация одобрена"
- ✅ Редирект на TaskDetails

**SQL проверка:**
```sql
SELECT status, approved_at, approved_by 
FROM task_submissions 
WHERE id = 'SUBMISSION_ID';
```

### Шаг 8: Просмотр прогресса
1. Обе стороны видят прогресс в TaskDetails

**Ожидаемый результат:**
- ✅ Блок "Прогресс выполнения"
- ✅ Прогресс-бары для просмотров, лайков, комментариев
- ✅ Текущие значения / целевые
- ✅ Текст "Метрики обновляются автоматически каждый час"

### Шаг 9: Симуляция обновления метрик (вручную)
Выполните в Supabase SQL Editor:
```sql
SELECT update_submission_progress(
  'SUBMISSION_ID'::uuid,
  50000,  -- текущие просмотры (50%)
  2500,   -- текущие лайки (50%)
  250     -- текущие комментарии (50%)
);
```

**Ожидаемый результат:**
- ✅ `current_metrics` обновлены
- ✅ Статус остается `in_progress` (не все метрики достигнуты)

**Проверка:**
```sql
SELECT current_metrics, status 
FROM task_submissions 
WHERE id = 'SUBMISSION_ID';
```

### Шаг 10: Достижение целей и автоплата
```sql
SELECT update_submission_progress(
  'SUBMISSION_ID'::uuid,
  100000,  -- 100% просмотров
  5000,    -- 100% лайков
  500      -- 100% комментариев
);
```

**Ожидаемый результат:**
- ✅ Статус submission → `completed`
- ✅ Статус task → `completed`
- ✅ Баланс заказчика уменьшен на task.budget
- ✅ Баланс инфлюенсера увеличен на task.budget
- ✅ Транзакция создана в `transactions`

**SQL проверка:**
```sql
-- Проверить статус
SELECT status FROM task_submissions WHERE id = 'SUBMISSION_ID';
SELECT status FROM tasks WHERE id = 'TASK_ID';

-- Проверить балансы
SELECT id, first_name, balance FROM users 
WHERE id IN ('CLIENT_ID', 'INFLUENCER_ID');

-- Проверить транзакцию
SELECT * FROM transactions 
WHERE task_id = 'TASK_ID' 
ORDER BY created_at DESC LIMIT 1;
```

## Сценарий 2: Отклонение публикации

### Шаг 1-5: Аналогично Сценарию 1
Дойдите до страницы ReviewSubmission

### Шаг 6: Заказчик отклоняет
1. На странице `/client/task/:taskId/review`
2. Нажмите "❌ Отклонить"
3. В модальном окне введите причину: "Пост не соответствует требованиям"
4. Нажмите "Отправить"

**Ожидаемый результат:**
- ✅ Статус → `rejected`
- ✅ `rejection_reason` сохранена
- ✅ `reviewed_at` заполнено

**SQL проверка:**
```sql
SELECT status, rejection_reason, reviewed_at 
FROM task_submissions 
WHERE id = 'SUBMISSION_ID';
```

### Шаг 7: Инфлюенсер видит отклонение
1. Откройте приложение как инфлюенсер
2. Перейдите к заданию

**Ожидаемый результат:**
- ✅ Красный баннер "❌ Публикация отклонена"
- ✅ Отображается причина отклонения
- ✅ Кнопка "📤 Отправить новую публикацию"

### Шаг 8: Повторная отправка
1. Исправьте пост в Instagram
2. Отправьте новую ссылку
3. Заказчик снова проверяет и одобряет

## Сценарий 3: Проверка RLS политик

### Попытка изменения чужого submission
```sql
-- От имени другого пользователя
UPDATE task_submissions 
SET status = 'in_progress' 
WHERE id = 'SUBMISSION_ID';
```

**Ожидаемый результат:**
- ❌ Ошибка: "new row violates row-level security policy"

### Попытка одобрения без прав
```javascript
// В консоли браузера (другой пользователь)
const { data, error } = await supabase.rpc('approve_submission', {
  p_submission_id: 'SUBMISSION_ID',
  p_client_id: 'WRONG_CLIENT_ID',
  p_approved: true
});
console.log(error); // Должна быть ошибка
```

**Ожидаемый результат:**
- ❌ "У вас нет прав для одобрения этой публикации"

## Сценарий 4: Edge Cases

### 1. Двойное одобрение
Попробуйте одобрить уже одобренную публикацию

**Ожидаемый результат:**
- ❌ Ошибка: "Публикация уже проверена"

### 2. Пустая причина отклонения
Нажмите "Отклонить" без ввода причины

**Ожидаемый результат:**
- ❌ Валидация на фронтенде требует причину

### 3. Невалидная ссылка Instagram
Попробуйте отправить `https://facebook.com/post`

**Ожидаемый результат:**
- ❌ Ошибка валидации: "Введите корректную ссылку Instagram"

### 4. Обновление метрик для не-approved submission
```sql
SELECT update_submission_progress(
  'PENDING_SUBMISSION_ID'::uuid,
  10000, 1000, 100
);
```

**Ожидаемый результат:**
- ❌ Ошибка или игнорирование (submission должен быть in_progress)

## Чек-лист функциональности

### UI/UX
- [ ] Желтый баннер появляется для заказчика при pending_approval
- [ ] Кнопка "Проверить публикацию" ведет на ReviewSubmission
- [ ] ReviewSubmission отображает всю информацию корректно
- [ ] Прогресс-бары обновляются в TaskDetails
- [ ] Уведомления при одобрении/отклонении работают

### Backend
- [ ] task_submissions таблица создана
- [ ] approve_submission() функция работает
- [ ] update_submission_progress() функция работает
- [ ] RLS политики запрещают несанкционированный доступ
- [ ] Транзакции создаются при автооплате

### Безопасность
- [ ] Только заказчик задания может одобрить submission
- [ ] Только инфлюенсер может создать submission для своих принятых задач
- [ ] Нельзя изменить статус напрямую через UPDATE
- [ ] Нельзя одобрить дважды

### Автоматизация
- [ ] При 100% всех метрик → auto-complete
- [ ] При auto-complete → автоматическая оплата
- [ ] Балансы обновляются корректно
- [ ] Транзакция записывается

## Следующие шаги

После успешного тестирования:
1. ✅ Создать Edge Function для Instagram API интеграции
2. ✅ Настроить Cron Job для обновления метрик каждый час
3. ✅ Добавить Telegram уведомления
4. ✅ Добавить графики прогресса
5. ✅ Тестирование на продакшене

## Известные проблемы

### Метрики обновляются только вручную
**Причина:** Instagram API интеграция еще не реализована  
**Временное решение:** Обновляйте вручную через SQL  
**План:** Создать Edge Function с cron job

### Нет уведомлений в Telegram
**Причина:** Telegram bot integration для уведомлений не добавлена  
**План:** Добавить отправку уведомлений через telegramBot.js

### Нет истории метрик
**Причина:** Сохраняются только текущие значения  
**План:** Создать таблицу submission_metrics_history для графиков
