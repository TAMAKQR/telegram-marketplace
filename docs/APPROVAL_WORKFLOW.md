# Workflow одобрения публикаций

## Обзор
Система одобрения публикаций обеспечивает контроль качества перед началом отслеживания метрик. Заказчик должен одобрить публикацию инфлюенсера, прежде чем начнется отслеживание просмотров, лайков и комментариев.

## Workflow

### 1. Инфлюенсер отправляет публикацию
**Страница:** `SubmitTaskPost.jsx` (`/influencer/task/:taskId/submit`)

**Действия:**
- Инфлюенсер создает пост в Instagram
- Копирует ссылку на публикацию
- Переходит на страницу отправки через кнопку "📤 Отправить публикацию" в TaskDetails
- Вставляет ссылку на публикацию
- Нажимает "Отправить на проверку"

**Что происходит:**
```javascript
// Создается запись в task_submissions
{
  task_id: taskId,
  influencer_id: user.id,
  post_url: "https://www.instagram.com/p/...",
  status: 'pending_approval', // Ожидает одобрения
  submitted_at: new Date()
}
```

### 2. Заказчик видит уведомление
**Страница:** `TaskDetails.jsx` 

**Отображение:**
- Желтый баннер с уведомлением "📤 Новая публикация на проверке"
- Кнопка "🔍 Проверить публикацию"
- Уведомление появляется когда `submission.status === 'pending_approval'`

### 3. Заказчик проверяет публикацию
**Страница:** `ReviewSubmission.jsx` (`/client/task/:taskId/review`)

**Отображение:**
- Ссылка на публикацию Instagram (открывается в новой вкладке)
- Информация об инфлюенсере
- Целевые метрики (просмотры, лайки, комментарии)
- Две кнопки: "✅ Одобрить" и "❌ Отклонить"

### 4. Одобрение или отклонение

#### Одобрение
**Функция:** `approve_submission()` (SQL function)

**Что происходит:**
1. Проверяется, что пользователь действительно заказчик этого задания
2. Статус submission меняется: `pending_approval` → `in_progress`
3. Статус задания остается `in_progress`
4. Начинается отслеживание метрик

```sql
UPDATE task_submissions 
SET 
  status = 'in_progress',
  approved_at = NOW(),
  approved_by = client_id
WHERE id = submission_id;
```

#### Отклонение
**Что происходит:**
1. Заказчик вводит причину отклонения
2. Статус submission меняется: `pending_approval` → `rejected`
3. Сохраняется причина отклонения
4. Инфлюенсер может подать новую публикацию

```sql
UPDATE task_submissions 
SET 
  status = 'rejected',
  rejection_reason = reason,
  reviewed_at = NOW()
WHERE id = submission_id;
```

### 5. Отслеживание метрик
**Статус:** `in_progress`

После одобрения заказчиком:
- Система начинает автоматически проверять метрики через Instagram API
- Функция `update_submission_progress()` обновляет текущие значения
- Прогресс отображается в TaskDetails для обеих сторон

**Отображение прогресса:**
```jsx
// Для каждой метрики показывается прогресс-бар
👁️ Просмотры: 45,230 / 100,000 (45%)
❤️ Лайки: 2,100 / 5,000 (42%)
💬 Комментарии: 150 / 500 (30%)
```

### 6. Автоматическое завершение
**Триггер:** Все метрики достигли 100%

**Функция:** `update_submission_progress()`

**Что происходит:**
1. Статус submission → `completed`
2. Статус задания → `completed`
3. **Автоматическая оплата:**
   - Списывается с баланса заказчика
   - Зачисляется на баланс инфлюенсера
   - Создается транзакция в `transactions`

```sql
-- Если все метрики >= 100%
UPDATE task_submissions SET status = 'completed';
UPDATE tasks SET status = 'completed';

-- Оплата
UPDATE users SET balance = balance - task.budget WHERE id = client_id;
UPDATE users SET balance = balance + task.budget WHERE id = influencer_id;
```

## Статусы submission

### `pending_approval`
- **Описание:** Инфлюенсер отправил публикацию, ожидает проверки
- **Кто видит:** Заказчик видит уведомление
- **Действия:** Заказчик может одобрить или отклонить

### `approved`
- **Описание:** Заказчик одобрил, но метрики еще не обновлялись
- **Переход:** Автоматически → `in_progress` после первого обновления метрик

### `in_progress`
- **Описание:** Идет отслеживание метрик
- **Отображение:** Прогресс-бары с текущими значениями
- **Завершение:** Когда все метрики >= 100%

### `rejected`
- **Описание:** Заказчик отклонил публикацию
- **Отображение:** Причина отклонения
- **Действия:** Инфлюенсер может отправить новую публикацию

### `completed`
- **Описание:** Все метрики достигнуты, оплата произведена
- **Финальный статус**

### `failed`
- **Описание:** Публикация удалена или недоступна
- **Использование:** Для технических проблем

## SQL Функции

### `approve_submission()`
```sql
CREATE OR REPLACE FUNCTION approve_submission(
  p_submission_id UUID,
  p_client_id UUID,
  p_approved BOOLEAN,
  p_rejection_reason TEXT DEFAULT NULL
)
```

**Параметры:**
- `p_submission_id` - ID публикации
- `p_client_id` - ID заказчика (для проверки прав)
- `p_approved` - true для одобрения, false для отклонения
- `p_rejection_reason` - причина отклонения (если p_approved = false)

**Проверки:**
- Заказчик действительно владелец задания
- Submission существует и в статусе pending_approval

### `update_submission_progress()`
```sql
CREATE OR REPLACE FUNCTION update_submission_progress(
  p_submission_id UUID,
  p_current_views INTEGER DEFAULT NULL,
  p_current_likes INTEGER DEFAULT NULL,
  p_current_comments INTEGER DEFAULT NULL
)
```

**Параметры:**
- `p_submission_id` - ID публикации
- Текущие значения метрик

**Логика:**
- Обновляет current_metrics JSONB
- Вычисляет процент выполнения для каждой метрики
- Если все >= 100% → автоматически завершает и оплачивает

## RLS Политики

### Инфлюенсеры
```sql
-- Могут создавать submissions для своих принятых задач
-- Могут читать свои submissions
```

### Заказчики
```sql
-- Могут читать submissions для своих задач
-- Могут обновлять статус через approve_submission()
```

### Публичный доступ
```sql
-- Нет публичного доступа к task_submissions
```

## Интеграция с Instagram API

### Получение метрик
**Endpoint:** `GET /{media-id}?fields=like_count,comments_count,insights.metric(impressions)`

**Требования:**
- Instagram Graph API
- Facebook Business Portfolio
- Access Token инфлюенсера

**Частота обновления:**
- Каждый час для submissions в статусе `in_progress`
- Через Edge Function или Cron Job

### Извлечение Media ID
```javascript
// Из URL: https://www.instagram.com/p/ABC123xyz/
const mediaShortcode = 'ABC123xyz';
// Нужно преобразовать в media_id через Instagram API
```

## Пользовательский опыт

### Для инфлюенсера
1. ✅ Принял задание
2. 📸 Создал пост в Instagram
3. 📤 Отправил ссылку через приложение
4. ⏳ Ждет одобрения заказчика
5. ✅ Получил одобрение
6. 📊 Отслеживает прогресс метрик
7. 💰 Автоматически получает оплату при достижении целей

### Для заказчика
1. ✅ Принял отклик инфлюенсера
2. 🔔 Получил уведомление о публикации
3. 🔍 Проверил публикацию в Instagram
4. ✅ Одобрил или ❌ Отклонил
5. 📊 Отслеживает прогресс
6. 💰 Автоматическая оплата при завершении

## Файлы системы

### Frontend
- `src/pages/SubmitTaskPost.jsx` - Страница отправки публикации инфлюенсером
- `src/pages/ReviewSubmission.jsx` - Страница проверки публикации заказчиком
- `src/pages/TaskDetails.jsx` - Обновлено для уведомлений и прогресса

### Backend (SQL)
- `supabase/task_tracking.sql` - Схема, функции, RLS политики

### Routes
```javascript
// App.jsx
<Route path="/influencer/task/:taskId/submit" element={<SubmitTaskPost />} />
<Route path="/client/task/:taskId/review" element={<ReviewSubmission />} />
```

## Будущие улучшения

### Приоритет 1 - Критично
- [ ] Edge Function для автоматического обновления метрик через Instagram API
- [ ] Cron job для периодической проверки (каждый час)
- [ ] Обработка ошибок Instagram API (deleted posts, private accounts)

### Приоритет 2 - Важно
- [ ] Уведомления в Telegram при одобрении/отклонении
- [ ] История изменения метрик (для графиков)
- [ ] Возможность повторной отправки после отклонения

### Приоритет 3 - Желательно
- [ ] Предпросмотр Instagram поста в приложении
- [ ] Аналитика роста метрик (скорость достижения целей)
- [ ] Прогнозируемая дата завершения
