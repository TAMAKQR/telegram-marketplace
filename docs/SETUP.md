# Инструкция по настройке проекта

## Шаг 1: Установка зависимостей

```bash
npm install
```

## Шаг 2: Настройка Supabase

### 2.1 Создание проекта

1. Перейдите на [supabase.com](https://supabase.com)
2. Нажмите "New Project"
3. Выберите организацию или создайте новую
4. Заполните данные проекта:
   - Name: telegram-influencer-marketplace
   - Database Password: придумайте надежный пароль
   - Region: выберите ближайший к вам регион
5. Нажмите "Create new project" и дождитесь создания (~2 минуты)

### 2.2 Создание таблиц

1. В левом меню выберите "SQL Editor"
2. Нажмите "New query"
3. Скопируйте содержимое файла `supabase/schema.sql`
4. Вставьте в редактор и нажмите "Run"
5. Убедитесь, что все таблицы созданы без ошибок

### 2.3 Получение ключей API

1. Перейдите в Settings → API
2. Скопируйте:
   - Project URL (например: `https://xxxxx.supabase.co`)
   - `anon` `public` key (длинный токен)

### 2.4 Настройка переменных окружения

1. Создайте файл `.env` в корне проекта
2. Скопируйте содержимое из `.env.example`
3. Замените значения на ваши:

```env
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here
```

## Шаг 3: Создание Telegram Bot

### 3.1 Создание бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Введите имя бота (например: "Influencer Marketplace")
4. Введите username бота (должен заканчиваться на `bot`, например: `influencer_market_bot`)
5. Сохраните полученный токен (понадобится позже)

### 3.2 Настройка Web App

1. Отправьте команду `/newapp` в BotFather
2. Выберите созданного бота
3. Введите название приложения
4. Введите описание
5. Загрузите иконку (опционально, квадратное изображение 640x640px)
6. Пока пропустите URL (настроим после деплоя)

## Шаг 4: Локальный запуск

```bash
npm run dev
```

Приложение будет доступно на `http://localhost:5173`

⚠️ **Важно**: Для полноценного тестирования Web App нужно открывать через Telegram!

## Шаг 5: Тестирование (локально через ngrok)

Для тестирования Web App локально:

### 5.1 Установите ngrok

```bash
# Windows (через chocolatey)
choco install ngrok

# Или скачайте с https://ngrok.com/download
```

### 5.2 Запустите туннель

```bash
ngrok http 5173
```

### 5.3 Настройте URL в BotFather

1. Скопируйте HTTPS URL из ngrok (например: `https://xxxx.ngrok.io`)
2. В BotFather отправьте `/myapps`
3. Выберите ваше приложение
4. "Edit Web App URL"
5. Вставьте URL от ngrok

### 5.4 Тестирование

1. Откройте вашего бота в Telegram
2. Нажмите на иконку меню (≡) рядом с полем ввода
3. Выберите ваше Web App
4. Приложение должно открыться!

## Шаг 6: Деплой на Vercel (Production)

### 6.1 Подготовка

```bash
npm run build
```

Проверьте, что сборка прошла успешно.

### 6.2 Деплой

```bash
# Установите Vercel CLI
npm install -g vercel

# Войдите в аккаунт
vercel login

# Деплой
vercel
```

Следуйте инструкциям:
- Выберите ваш проект или создайте новый
- Подтвердите настройки

### 6.3 Добавьте переменные окружения в Vercel

1. Перейдите в проект на vercel.com
2. Settings → Environment Variables
3. Добавьте:
   - `VITE_SUPABASE_URL`
   - `VITE_SUPABASE_ANON_KEY`
4. Выберите все окружения (Production, Preview, Development)
5. Сохраните

### 6.4 Пересоберите проект

```bash
vercel --prod
```

### 6.5 Обновите URL в BotFather

1. Скопируйте production URL (например: `https://your-app.vercel.app`)
2. В BotFather: `/myapps` → Edit Web App URL
3. Вставьте новый URL

## Шаг 7: Тестирование Production

1. Откройте бота в Telegram
2. Откройте Web App
3. Проверьте все функции:
   - Выбор типа пользователя
   - Создание задания (для заказчика)
   - Создание профиля Instagram (для инфлюенсера)
   - Отклики на задания
   - Просмотр статистики

## Возможные проблемы и решения

### "Missing Supabase environment variables"

- Проверьте, что файл `.env` создан и содержит правильные ключи
- Перезапустите dev сервер после изменения `.env`

### Web App не открывается в Telegram

- Убедитесь, что URL настроен в BotFather
- Проверьте, что URL доступен (откройте в браузере)
- HTTPS обязателен для Web Apps!

### Ошибки базы данных

- Проверьте, что все таблицы созданы в Supabase
- Проверьте Row Level Security политики
- Посмотрите логи в Supabase Dashboard

### Стили не применяются

```bash
# Пересоберите Tailwind
npm run dev
```

## Дополнительные настройки

### Настройка Telegram Bot команд

В BotFather отправьте `/setcommands` и добавьте:

```
start - Запустить приложение
help - Помощь
```

### Настройка описания бота

```
/setdescription - Полное описание
/setabouttext - Краткое описание
/setuserpic - Аватар бота
```

## Полезные ссылки

- [Supabase Documentation](https://supabase.com/docs)
- [Telegram Web Apps](https://core.telegram.org/bots/webapps)
- [Vercel Documentation](https://vercel.com/docs)
- [React Documentation](https://react.dev)

---

Готово! 🎉 Ваш Telegram Web App для маркетплейса инфлюенсеров готов к использованию!
